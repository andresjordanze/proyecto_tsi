<p id="notice"><%= notice %></p>
<CENTER>
<p>
  <b>Nombre:</b>
  <%= @category.name %>
</p>

<%= link_to 'Editar', edit_category_path(@category), class: "btn btn-primary"  %>
<br><br>
<%= button_to 'Volver', categories_path, :method => :get, class: "btn btn-primary"  %>
</CENTER>
