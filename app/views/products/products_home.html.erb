<% title "Crear Producto" %>
<h1>Todos Los Productos</h1>

<h3><%= link_to 'Añadir Producto', products_path %></h3>
<CENTER>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><CENTER>Descripcion</CENTER></th>
        <th><CENTER>Foto</CENTER></th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <% @products.each do |product| %>
      <%if product.home == true%>
        <tr>
          <td><CENTER><%= product.description %></CENTER></td>
          <td><CENTER><img src= '<%=product.photo.url %>' width=60 height=60></a></CENTER></td>
          <td><%= link_to 'Ver',  :controller => :products, :action => 'view_product', :id => product.id%></td>

          <td><%= link_to 'Editar',  :controller => :products, :action => 'edit_to_home', :id => product.id%></td>
          <td><%= button_to 'Eliminar Producto de pagina', :controller => :products, :action => 'delete_from_home', :id => product.id %></td>
      <%end%>
        </tr>
    <% end %>
  </table>
  <br>
</Center>

