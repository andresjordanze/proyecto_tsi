<%= form_validated_for(@brand) do |f| %>
<CENTER>
  <p>
    <%= f.label :name, "*Nombre de Marca" %>
    <%= f.text_field :name, :maxlength => '20'%>
    </p>
  
	<p><%= f.submit (@brand.new_record? ? 'Crear Marca' : 'Guardar Cambios'), :class => 'btn btn-lg btn-success' %></p>
<% end %>
</CENTER>