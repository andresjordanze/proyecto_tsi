<%= form_validated_for(@category) do |f| %>
<CENTER>
  <p>
    <%= f.label :name, "Nombre de la categoria" %>
    <%= f.text_field :name :maxlength => '20'%>
    </p>
  
  <p>
    <%= f.submit "Guardar Categoria", class: "btn btn-large btn-primary" %>
  </p>
<% end %>
</CENTER>